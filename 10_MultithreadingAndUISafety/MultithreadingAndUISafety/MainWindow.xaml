<Window
    x:Class="MultithreadingAndUISafety.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:behaviors="clr-namespace:MultithreadingAndUISafety.Behaviors"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:MultithreadingAndUISafety"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="clr-namespace:MultithreadingAndUISafety.ViewModels"
    Title="MainWindow"
    Width="800"
    Height="450"
    d:DataContext="{d:DesignInstance Type=viewmodels:MainViewModel}"
    mc:Ignorable="d">
    <Window.DataContext>
        <viewmodels:MainViewModel />
    </Window.DataContext>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel
            Margin="0,0,0,10"
            HorizontalAlignment="Center"
            Orientation="Horizontal">
            <StackPanel.Resources>
                <Style TargetType="Button">
                    <Setter Property="Width" Value="120" />
                    <Setter Property="Height" Value="35" />
                    <Setter Property="FontSize" Value="14" />
                </Style>
            </StackPanel.Resources>
            <Button
                Margin="0,0,5,0"
                Background="LightGreen"
                Command="{Binding StartCommand}"
                Content="开始处理" />
            <Button
                Background="LightCoral"
                Command="{Binding CancelCommand}"
                Content="取消任务" />
        </StackPanel>
        <TextBlock
            Grid.Row="1"
            Margin="0,0,0,10"
            FontSize="16"
            FontWeight="Bold"
            Foreground="#333"
            Text="{Binding StatusText}" />
        <ProgressBar
            Grid.Row="2"
            Margin="0,0,0,10"
            Maximum="100"
            Minimum="0"
            Value="{Binding ProgressValue}" />
        <GroupBox Grid.Row="3" Header="实时日志">
            <ListBox
                behaviors:AutoScrollBehavior.Enable="True"
                BorderThickness="0"
                ItemsSource="{Binding RealTimeLogs}"
                VirtualizingPanel.IsVirtualizing="True"
                VirtualizingPanel.VirtualizationMode="Recycling">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" />
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </GroupBox>
    </Grid>
</Window>
