<Window
    x:Class="APAndBehaviorSystem.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:behavior="clr-namespace:APAndBehaviorSystem.Behaviors"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:APAndBehaviorSystem"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:APAndBehaviorSystem.ViewModels"
    Title="MainWindow"
    Width="800"
    Height="450"
    mc:Ignorable="d">
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>
    <StackPanel
        Margin="20"
        HorizontalAlignment="Center"
        VerticalAlignment="Center">
        <TextBox
            Width="150"
            Height="35"
            VerticalContentAlignment="Center"
            behavior:CommandBehavior.EnterKeyCommand="{Binding SubmitCommand}"
            behavior:FocusBehavior.AutoSelectAll="True"
            behavior:InputBehavior.OnlyNumeric="True" />
        <TextBlock
            Margin="0,20,0,0"
            FontSize="16"
            Text="{Binding Message}" />
    </StackPanel>
</Window>
