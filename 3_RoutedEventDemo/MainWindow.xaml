<Window
    x:Class="RoutedEventDemo.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ctrl="clr-namespace:RoutedEventDemo.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:RoutedEventDemo"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="MainWindow"
    Width="800"
    Height="450"
    mc:Ignorable="d">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Margin="0,0,0,10" Orientation="Horizontal">
            <Button
                Width="80"
                Margin="5"
                Click="Info_Click"
                Content="Info" />
            <Button
                Width="80"
                Margin="5"
                Click="Warning_Click"
                Content="Warning" />
            <Button
                Width="80"
                Margin="5"
                Click="Error_Click"
                Content="Error" />
            <Button
                Width="80"
                Margin="5"
                Click="Pause_Click"
                Content="Pause" />
            <Button
                Width="80"
                Margin="5"
                Click="Resume_Click"
                Content="Resume" />
            <Button
                Width="80"
                Margin="5"
                Click="ClearLog_Click"
                Content="ClearLog" />
            <Button
                Width="80"
                Margin="5"
                Click="ExportLog_Click"
                Content="ExportLog" />
        </StackPanel>

        <StackPanel
            Grid.Row="1"
            Margin="0,0,0,10"
            Orientation="Horizontal">
            <TextBlock VerticalAlignment="Center" Text="Level:" />
            <ComboBox
                Width="120"
                Margin="5,0"
                SelectionChanged="LevelChanged">
                <ComboBoxItem Content="Info" />
                <ComboBoxItem Content="Warning" />
                <ComboBoxItem Content="Error" />
            </ComboBox>
            <TextBlock VerticalAlignment="Center" Text="Search:" />
            <TextBox
                Width="120"
                Margin="5,0"
                TextChanged="SearchChanged" />
        </StackPanel>

        <ctrl:LogPanel x:Name="logPanel" Grid.Row="2">
            <ctrl:LogPanel.ItemTemplate>
                <DataTemplate>
                    <TextBlock FontFamily="Consolas" Text="{Binding}">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="White" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Level}" Value="Info">
                                        <Setter Property="Foreground" Value="LightGreen" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Level}" Value="Warning">
                                        <Setter Property="Foreground" Value="Orange" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Level}" Value="Error">
                                        <Setter Property="Foreground" Value="Red" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </DataTemplate>
            </ctrl:LogPanel.ItemTemplate>
        </ctrl:LogPanel>
    </Grid>
</Window>
